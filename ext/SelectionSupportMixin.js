//*@public
/**
    Applied to _enyo.CollectionControllers_ to be able to handle support
    for selection of indeces.
*/
enyo.Mixin({
  //*@protected
  name: "enyo.SelectionSupportMixin",
  //*@public
  /**
    Supply these properties to indicate if selection support is needed
    and whether it is mulitple selection support or single selection
    support.
  */
  //*@protected
  initMixin: function () {
      
  },
});
